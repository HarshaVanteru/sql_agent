# API
fastapi
uvicorn

# Agent
langchain-core
langchain-groq

# Database
sqlalchemy
alembic
aiomysql          # app database, async (DATABASE_URL)
pymysql           # app database for migrations; also MySQL targets the agent queries
psycopg2-binary   # PostgreSQL targets the agent queries
asyncpg           # only needed if the app database itself is PostgreSQL

# Auth
python-jose       # JWT encode/decode
bcrypt            # password hashing
cryptography      # Fernet, for encrypting stored connection passwords

# Config and validation
pydantic
email-validator   # required by pydantic's EmailStr
python-dotenv
