# Where the backend is served from. Must be an origin the API lists in
# CORS_ORIGINS -- the session is a cookie, so credentials are sent with every
# request and a wildcard origin is not usable.
VITE_API_URL=http://localhost:8000
